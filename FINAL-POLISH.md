# 마지막 화면 보완

## 사용 흐름

- 홈에서 장르 또는 순수 랜덤을 선택하고 보틀 색을 고릅니다.
- 순수 랜덤은 같은 선택을 한 사람끼리 음악 장르에 제한 없이 교환합니다. 특정 장르와 해류를 고른 사용자의 매칭 조건은 유지됩니다.
- 받은 보틀에서 `병 편지 열기`를 누르면 곡 정보가 나타납니다.
- 설정에서 획득한 도전과제 칭호를 장착하고, 교환 장르와 최근 7일 그래프를 확인합니다.
- 왼쪽 `모두의 바다`에서 최근 공개 교환 소식 24건이 반복해서 흐릅니다. 모바일에서는 하단 탭에 있습니다.
- 소식은 일시정지하거나 목록으로 볼 수 있습니다. 기기의 동작 줄이기 설정을 따릅니다.

## 수정 위치

- `src/voyage.tsx`: 병 색상 선택, 도착 연출, 교환 소식, 기록 그래프.
- `src/voyage.css`: 색상 필터, 파도, 병 움직임, 소식 이동 속도와 화면 크기 대응.
- `shared/rules.js`: 장르와 색상 입력 검증.
- `server/store.js`: 병 색 저장, 칭호 장착 검증, 공개 교환 소식.
- `server/progress.js`: 레벨과 도전과제 조건. 달성한 도전과제 이름은 칭호로 선택 가능합니다.

## 데이터

기존 SQLite 기록을 유지하며 색과 칭호 열을 추가합니다. 공개 소식에는 장르, 병 색, 완료 시각만 표시합니다. 사용자 ID, 노래 URL, 개인 메시지는 반환하지 않고, 해류 코드가 있는 교환과 신고된 교환은 제외합니다.

그래프는 완료된 내 교환만 한 번씩 집계합니다. 순수 랜덤은 실제 곡 장르를 추측하지 않고 별도 항목으로 표시합니다.
